import React from "react";
import renderer from "react-test-renderer";
import GetImageButton from "./get-image-button";

/* it("renders GET image button component correctly", () => {
	const tree = renderer
		.create(<GetImageButton getApiUrl="" text="get image" />)
		.toJSON();
	expect(tree).toMatchSnapshot();
}); */

it("GET Image button sample test", () => {
	expect(true).toBeTruthy();
});
